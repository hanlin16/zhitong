<#include "./common-conf.ftl" parse=true encoding="utf-8">
<!-- Tell the browser to be responsive to screen width -->
  <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
  <!-- Bootstrap 3.3.6 -->
  <link rel="stylesheet" href="${ctx }/plugins/bootstrap/css/bootstrap.min.css?ver=${version}">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="${ctx }/plugins/font-awesome/css/font-awesome.min.css?ver=${version}">
  <link href="${ctx }/plugins/bootstrap3-dialog/css/bootstrap-dialog.min.css?ver=${version}" rel="stylesheet" type="text/css" />
  <!-- Theme style -->
  <link rel="stylesheet" href="${ctx }/css/AdminLTE.css?ver=${version}">
  <!-- AdminLTE Skins. Choose a skin from the css/skins
       folder instead of downloading all of them to reduce the load. -->
  
  <link rel="stylesheet" href="${ctx }/css/skins/skin-blue.min.css?ver=${version}">
  
  <link rel="stylesheet" href="${ctx }/plugins/toastr/toastr.css?ver=${version}">
	
<link rel="stylesheet" type="text/css" media="screen" href="${ctx }/plugins/jqgrid/ui.jqgrid-bootstrap.css" />
  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
  <script src="${ctx }/plugins/html5shiv.min.js"></script>
  <script src="${ctx }/plugins/respond.min.js"></script>
  <![endif]-->